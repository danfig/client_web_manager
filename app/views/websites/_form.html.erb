<%= simple_form_for @website, :html => { :class => 'form-horizontal' } do |f| %>
  <% if @website.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@website.errors.count, "error") %> prohibited this website from being saved:</h2>

      <ul>
      <% @website.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.input :name, :input_html => { :class => "span6" },
              :placeholder => "e.g. School Boy Mistakes" %>

    <%= f.input :url, :input_html => { :class => "span6" },
              :placeholder => "e.g. http://schoolboy-mistakes.com" %>
  
   <%= f.input :client_id, collection: Client.all %>
   
 <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= submit_tag 'Cancel', :type => :reset, :class => "btn btn-danger" %>
  </div>
<% end %>
